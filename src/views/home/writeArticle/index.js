import React from 'react'
import { Form, Input, Select, Button } from 'antd'

class writeArticle extends React.Component{
    constructor(props){
        super(props)
    }
    render(){
        return(
            <Form>

            </Form>
        )
    }
}

export default Form.create({name:"myFrom"})(writeArticle)